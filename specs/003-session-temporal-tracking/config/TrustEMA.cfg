\* TLC Model Checker Configuration for TrustEMA specification
\*
\* Uses integer scaling: 0-100 represents 0.0-1.0 range
\*

CONSTANTS
    ALPHA = 30
    TRUST_THRESHOLD = 30
    MAX_TURNS = 8

SPECIFICATION Spec

\* State space constraint
CONSTRAINT StateConstraint

\* Invariants to check (TLC-compatible state predicates only)
INVARIANTS
    TypeOK
    TrustBounded
    TurnCountBounded
    HistoryConsistent
    TrustMonitoringOK
    HighTrustPattern
    DegradedTrustPattern
    ConvergencePattern
